# Progress

Track your progress through the masterclass. Update this file as you complete modules - Claude Code reads this to understand where you are in the project.

## Convention
- `[ ]` = Not started
- `[-]` = In progress
- `[x]` = Completed

## Modules

### Module 1: App Shell + Observability

Implementation landed in-repo — apply the migration on **your** Supabase project and configure `.env` to run end-to-end.

- [x] SQL migration + RLS (`supabase/migrations/…`)
- [x] FastAPI JWT auth, SSE chat proxy, Responses API + per-conversation vector stores
- [x] LangSmith `@traceable` on streamed `responses.create`
- [x] React chat UI (new chat, SSE tokens, file attach → vector store)

### Module 2: BYO Retrieval + Memory
